import React from "react";
import "./products.css";
import axios from "axios";
const ProductCard = ({ id, name }) => {
  //   console.log(props);

  return <div>{name}</div>;
};

export default ProductCard;
